setwd("C:/Users/ayesh/class/MechaCar_Statistical_Analysis")
library(dplyr)
mechacars <- read.csv('MechaCar_mpg.csv',stringsAsFactors = F) #read in dataset
View(mechacars)
#
# Perform linear regression using the lm() function
lm(mpg ~ vehicle_length + vehicle_weight + spoiler_angle + ground_clearance + AWD,data=mechacars) #generate multiple linear regression
summary(lm(mpg ~ vehicle_length + vehicle_weight + spoiler_angle + ground_clearance + AWD, data= mechacars)) #determine the p-value and the r-squared value for the linear regression model.
View(mechacars)
